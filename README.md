# Self-Evolving-Hash-Function
This project implements a Self-Evolving Hash Function (SEHF) for blockchain systems. Unlike normal hash functions that always produce the same output for the same input, SEHF produces a different hash for the same data depending on which block it is in. 
